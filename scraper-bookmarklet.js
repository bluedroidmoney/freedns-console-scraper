javascript:(async()=>{alert("🚀 FreeDNS Scraper started! Please stay on this page until the download appears.");console.log("Scraper started...");let allData=[];const pageText=document.body.innerText,pageMatch=pageText.match(/Page \d+ of (\d+)/),totalPages=pageMatch?parseInt(pageMatch[1]):1;for(let i=1;i<=totalPages;i++){const url=`https://freedns.afraid.org/domain/registry/page-${i}.html`;try{const resp=await fetch(url),html=await resp.text(),doc=new DOMParser().parseFromString(html,'text/html'),rows=doc.querySelectorAll('tr.trd, tr.trl');rows.forEach(row=>{const cols=row.querySelectorAll('td');if(cols.length>=4){const dFull=cols[0].innerText.trim(),domain=dFull.split(/\s+/)[0],hMatch=dFull.match(/\((\d+) hosts/);let hosts=hMatch?hMatch[1]:"0";const status=cols[1].innerText.trim(),ownerL=cols[2].querySelector('a'),owner=ownerL?ownerL.innerText.trim():cols[2].innerText.trim(),age=cols[3].innerText.trim();if(domain.includes('.'))allData.push({domain,status,owner,age,hosts})}});console.log(`Parsed Page ${i}/${totalPages}`);await new Promise(r=>setTimeout(r,400))}catch(e){console.error(`Error on page ${i}:`,e)}}allData.sort((a,b)=>a.domain.toLowerCase().localeCompare(b.domain.toLowerCase()));const txtContent=allData.map(item=>item.domain).join('\n'),blob=new Blob([txtContent],{type:'text/plain'}),link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download='freedns_domains.txt';link.click();alert(`✅ Done! Collected ${allData.length} domains. Check your downloads!`);})();
